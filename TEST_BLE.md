# BLE 温湿度传感器测试指南

## 🎯 测试目标

使用 MacBook 模拟 BLE 温湿度传感器，用 iPhone App 连接并读取实时数据。

## 📋 测试流程

### 第一步：启动 MacBook 模拟器

```bash
cd ble-simulator
npm install
npm start
```

看到以下输出表示成功：
```
🌡️  BLE 温湿度传感器模拟器
================================
🔵 蓝牙状态: poweredOn
🚀 开始广播: "TempHumiditySensor"
✅ 广播成功!
📱 现在可以用 iPhone App 连接了!
```

### 第二步：iPhone App 连接

1. **打开 App** → 首页
2. **点击 "蓝牙通讯"**
3. **点击 "开始扫描"**
4. **找到 "TempHumiditySensor"** 设备
5. **点击 "连接"**

### 第三步：查看数据

连接成功后，App 会自动：
- ✅ 发现设备的服务和特征值
- ✅ 读取温度和湿度数据
- ✅ 显示实时数据卡片

### 第四步：订阅实时更新

点击 **"订阅温度更新"** 或 **"订阅湿度更新"** 按钮，数据会每 2 秒自动更新。

## 📊 预期结果

### 设备信息
- 名称: TempHumiditySensor
- 服务 UUID: 181A (环境传感服务)
- 温度特征值: 2A6E
- 湿度特征值: 2A6F

### 实时数据
- 温度: 15-35°C（随机波动）
- 湿度: 30-90%（随机波动）

### 控制台日志

**MacBook 端：**
```
🔗 设备已连接: xx:xx:xx:xx:xx:xx
📖 读取温度: 25.3°C
📖 读取湿度: 62.5%
📡 订阅温度更新
```

**iPhone 端 (Xcode 控制台)：**
```
蓝牙状态变化: PoweredOn
连接成功: TempHumiditySensor
读取温度: 25.3°C
读取湿度: 62.5%
```

## 🔧 故障排除

### 问题 1: 扫描不到设备

**可能原因：**
- MacBook 蓝牙未开启
- 模拟器未启动
- 距离太远（> 10 米）

**解决方法：**
1. 检查 MacBook 蓝牙状态
2. 确认模拟器输出 "✅ 广播成功!"
3. 靠近 MacBook 再试

### 问题 2: 连接失败

**可能原因：**
- macOS 蓝牙权限问题
- App 蓝牙权限未授权

**解决方法：**
1. 系统设置 → 隐私与安全 → 蓝牙 → 允许终端访问
2. iPhone 设置 → 大苹果iot → 蓝牙 → 开启

### 问题 3: 读取数据失败

**可能原因：**
- 设备断开连接
- 特征值 UUID 不匹配

**解决方法：**
1. 检查连接状态
2. 重新连接设备
3. 查看 Xcode 控制台错误信息

## 📚 技术说明

### BLE 通信流程

```
iPhone App                          MacBook 模拟器
    │                                    │
    │──── 1. 扫描广播 ──────────────────>│
    │<─── 2. 广播响应 ───────────────────│
    │                                    │
    │──── 3. 连接请求 ──────────────────>│
    │<─── 4. 连接确认 ───────────────────│
    │                                    │
    │──── 5. 发现服务 ──────────────────>│
    │<─── 6. 服务列表 ───────────────────│
    │                                    │
    │──── 7. 发现特征值 ────────────────>│
    │<─── 8. 特征值列表 ─────────────────│
    │                                    │
    │──── 9. 读取温度 ──────────────────>│
    │<─── 10. 温度数据 ──────────────────│
    │                                    │
    │──── 11. 订阅湿度 ─────────────────>│
    │<─── 12. 湿度更新 (每2秒) ──────────│
```

### 数据格式

温度和湿度使用 **IEEE 11073** 格式：
- 16-bit 有符号整数 (温度)
- 16-bit 无符号整数 (湿度)
- 实际值 = 原始值 / 100

## 🎉 测试完成

成功测试后，你将了解：
- ✅ BLE 设备扫描和发现
- ✅ BLE 服务和特征值
- ✅ BLE 数据读取
- ✅ BLE 订阅通知
- ✅ 实时数据更新

这是 IoT 设备通信的基础，掌握后可以对接真实的智能硬件！
