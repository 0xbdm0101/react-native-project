# MQTT 配置保存功能测试指南

## 功能说明

配置保存功能允许你：
- ✅ 保存当前 Broker 配置
- ✅ 管理多个 Broker 配置
- ✅ 快速切换已保存的配置
- ✅ 自动加载上次的配置

---

## 测试步骤

### 测试 1：保存当前配置

**目标**：验证配置保存功能

**步骤**：
1. 打开 App，进入 MQTT 页面
2. 确认当前 Broker 配置（默认是 `broker.emqx.io:8084`）
3. 点击「保存配置」按钮
4. 输入配置名称，如：`EMQX 公共 Broker`
5. 点击「保存」按钮

**预期结果**：
- ✅ 配置保存成功
- ✅ 「已保存」按钮显示数量变为 1
- ✅ 配置名称正确显示

---

### 测试 2：查看已保存配置

**目标**：验证配置列表显示

**步骤**：
1. 点击「已保存 (1)」按钮
2. 查看配置列表

**预期结果**：
- ✅ 显示已保存的配置
- ✅ 显示配置名称：`EMQX 公共 Broker`
- ✅ 显示 Broker 地址：`broker.emqx.io:8084`
- ✅ 显示连接次数：0 次
- ✅ 显示保存时间

---

### 测试 3：连接并更新连接次数

**目标**：验证连接次数统计

**步骤**：
1. 关闭配置列表
2. 点击「连接」按钮
3. 等待连接成功
4. 再次查看已保存配置

**预期结果**：
- ✅ 连接成功
- ✅ 配置列表中显示连接次数：1 次
- ✅ 显示最后连接时间

---

### 测试 4：保存多个配置

**目标**：验证多配置管理

**步骤**：
1. 点击「编辑」按钮（铅笔图标）
2. 修改 Broker 配置：
   - 名称：`HiveMQ`
   - 地址：`broker.hivemq.com`
   - 端口：`8000`
   - 协议：`WS`
3. 点击「连接」按钮
4. 连接成功后，点击「保存配置」
5. 输入名称：`HiveMQ 公共 Broker`
6. 保存配置

**预期结果**：
- ✅ 新配置保存成功
- ✅ 「已保存」按钮显示数量变为 2
- ✅ 配置列表显示两个配置

---

### 测试 5：切换配置

**目标**：验证配置切换功能

**步骤**：
1. 点击「已保存 (2)」按钮
2. 点击第一个配置：`EMQX 公共 Broker`
3. 观察 Broker 卡片变化

**预期结果**：
- ✅ 配置列表关闭
- ✅ Broker 卡片显示：`broker.emqx.io:8084`
- ✅ 如果当前已连接，自动断开
- ✅ 可以点击「连接」使用新配置

---

### 测试 6：删除配置

**目标**：验证配置删除功能

**步骤**：
1. 点击「已保存 (2)」按钮
2. 点击某个配置的删除按钮（红色垃圾桶图标）
3. 确认删除

**预期结果**：
- ✅ 弹出确认对话框
- ✅ 确认后配置被删除
- ✅ 「已保存」按钮数量减 1
- ✅ 配置列表更新

---

### 测试 7：清空所有配置

**目标**：验证清空功能

**步骤**：
1. 点击「已保存」按钮
2. 点击右上角「清空」按钮
3. 确认清空

**预期结果**：
- ✅ 所有配置被删除
- ✅ 「已保存」按钮显示数量变为 0
- ✅ 配置列表为空

---

### 测试 8：自动加载配置

**目标**：验证启动时自动加载

**步骤**：
1. 保存一个配置
2. 关闭 App
3. 重新打开 App
4. 进入 MQTT 页面

**预期结果**：
- ✅ Broker 卡片显示上次保存的配置
- ✅ 已保存的配置列表还在
- ✅ 无需重新输入配置

---

### 测试 9：配置持久化

**目标**：验证配置在 App 重启后仍然存在

**步骤**：
1. 保存多个配置
2. 完全关闭 App（从后台清除）
3. 重新打开 App
4. 进入 MQTT 页面
5. 查看已保存配置

**预期结果**：
- ✅ 所有配置都还在
- ✅ 配置名称、地址、端口都正确
- ✅ 连接次数统计保留

---

## 测试记录表

| 测试项 | 状态 | 备注 |
|--------|------|------|
| 保存当前配置 | ⬜ | |
| 查看已保存配置 | ⬜ | |
| 连接次数统计 | ⬜ | |
| 保存多个配置 | ⬜ | |
| 切换配置 | ⬜ | |
| 删除配置 | ⬜ | |
| 清空所有配置 | ⬜ | |
| 自动加载配置 | ⬜ | |
| 配置持久化 | ⬜ | |

---

## 功能特点

### 1. 自动保存
- 连接成功后自动保存当前配置
- 更新连接次数和最后连接时间

### 2. 多配置管理
- 最多保存 10 个配置
- 支持命名和描述
- 显示连接统计

### 3. 快速切换
- 一键切换配置
- 自动断开当前连接
- 显示配置详情

### 4. 数据持久化
- 使用 AsyncStorage 本地存储
- App 重启后配置保留
- 支持导入/导出（未来功能）

---

## 常见问题

### Q1: 配置保存后在哪里？

**位置**：App 本地存储（AsyncStorage）

**查看方式**：
- 点击「已保存」按钮
- 所有保存的配置都在列表中

---

### Q2: 为什么保存配置后连接次数是 0？

**原因**：保存时还没有连接

**解决**：
- 连接一次后，连接次数会更新为 1
- 每次连接都会增加计数

---

### Q3: 如何修改已保存的配置？

**方法**：
1. 从列表中选择配置
2. 点击「编辑」按钮
3. 修改配置
4. 重新保存（会更新现有配置）

---

### Q4: 配置会丢失吗？

**不会**，除非：
- 手动删除配置
- 清空所有配置
- 卸载 App

**建议**：
- 重要配置可以截图保存
- 未来版本会支持导出功能

---

## 下一步

测试完成后，可以继续：
- 消息筛选和搜索功能
- 消息导出功能
- 真实设备对接（ESP32）

---

## 技术实现

### 存储结构
```typescript
interface SavedBroker extends MQTTBroker {
  alias: string;           // 配置名称
  lastConnected: number;   // 最后连接时间
  connectCount: number;    // 连接次数
}
```

### 存储位置
- 前缀：`MQTT_`
- 键：`current_broker`、`saved_brokers`
- 存储：AsyncStorage

### 限制
- 最多保存 10 个配置
- 配置大小限制（AsyncStorage 限制）
