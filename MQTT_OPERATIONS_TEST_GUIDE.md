# MQTT 设备控制页面测试指南

## 测试目标

验证设备控制页面的功能：
- ✅ 页面跳转正常
- ✅ 设备开关控制正常
- ✅ MQTT 消息发送正常
- ✅ 界面显示正常

---

## 测试步骤

### 测试 1：页面跳转

**步骤**：
1. 打开 App，进入 MQTT 页面
2. 滚动到底部
3. 点击「设备控制」按钮

**预期结果**：
- ✅ 成功跳转到设备控制页面
- ✅ 页面标题显示"设备控制"
- ✅ 返回按钮正常工作

---

### 测试 2：连接状态显示

**步骤**：
1. 进入设备控制页面
2. 观察顶部状态栏

**预期结果**：
- ✅ 如果未连接，显示红色指示灯和"未连接"
- ✅ 如果已连接，显示绿色指示灯和"已连接"
- ✅ 未连接时显示「连接」按钮

---

### 测试 3：连接 Broker

**步骤**：
1. 确保显示"未连接"
2. 点击「连接」按钮
3. 等待几秒

**预期结果**：
- ✅ 按钮显示加载状态
- ✅ 连接成功后显示"已连接"
- ✅ 指示灯变为绿色

---

### 测试 4：设备开关控制

**步骤**：
1. 确保已连接
2. 找到"客厅灯"设备
3. 点击右侧的开关按钮

**预期结果**：
- ✅ 开关状态切换
- ✅ 设备名称颜色变化（开启时为绿色）
- ✅ 下方"当前状态"更新

---

### 测试 5：查看发送记录

**步骤**：
1. 切换几个设备的开关
2. 滚动到"当前状态"部分
3. 查看设备状态

**预期结果**：
- ✅ 显示所有设备的当前状态
- ✅ 状态文字正确（开启/关闭）
- ✅ 主题地址正确显示

---

### 测试 6：MQTTX 验证

**步骤**：
1. 打开 MQTTX，连接到同一个 Broker
2. 订阅主题：`home/livingroom/light`
3. 在 App 中切换"客厅灯"开关
4. 查看 MQTTX 收到的消息

**预期结果**：
- ✅ MQTTX 收到消息
- ✅ 消息内容为 `{"state": "ON"}` 或 `{"state": "OFF"}`
- ✅ 消息格式正确

---

### 测试 7：多个设备控制

**步骤**：
1. 依次切换所有设备的开关
2. 观察每个设备的状态变化
3. 查看"当前状态"部分

**预期结果**：
- ✅ 每个设备独立控制
- ✅ 状态正确更新
- ✅ 界面响应流畅

---

### 测试 8：返回主页面

**步骤**：
1. 点击左上角返回按钮
2. 观察页面跳转

**预期结果**：
- ✅ 成功返回 MQTT 主页面
- ✅ 连接状态保持
- ✅ 之前的配置还在

---

## 测试记录表

| 测试项 | 状态 | 备注 |
|--------|------|------|
| 页面跳转 | ⬜ | |
| 连接状态显示 | ⬜ | |
| 连接 Broker | ⬜ | |
| 设备开关控制 | ⬜ | |
| 查看发送记录 | ⬜ | |
| MQTTX 验证 | ⬜ | |
| 多个设备控制 | ⬜ | |
| 返回主页面 | ⬜ | |

---

## 常见问题

### Q1: 点击开关没反应？
**可能原因**：
- 未连接到 Broker
- 网络问题

**解决方法**：
1. 检查连接状态
2. 点击「连接」按钮
3. 检查网络连接

---

### Q2: MQTTX 收不到消息？
**可能原因**：
- 主题不匹配
- Broker 不同

**解决方法**：
1. 确认 MQTTX 订阅的主题正确
2. 确认连接到同一个 Broker
3. 检查 MQTTX 连接状态

---

### Q3: 页面显示不全？
**可能原因**：
- 屏幕尺寸问题
- 滚动问题

**解决方法**：
1. 尝试滚动页面
2. 检查是否有内容被截断
3. 反馈具体问题

---

## 下一步

测试完成后，可以：
- 继续开发消息筛选和搜索功能
- 测试真实设备对接（ESP32）
- 优化界面和交互

---

## 技术说明

### 消息格式
每个设备发送的消息格式为 JSON：
```json
{
  "state": "ON",  // 或 "OFF"
  "speed": 3,     // 可选参数
  "temp": 26      // 可选参数
}
```

### 主题结构
- `home/livingroom/light` - 客厅灯
- `home/livingroom/fan` - 风扇
- `home/bedroom/ac` - 空调
- `home/livingroom/curtain` - 窗帘
- `home/livingroom/tv` - 电视
- `home/livingroom/speaker` - 音响

### QoS 设置
默认使用 QoS 1（至少一次送达），确保消息可靠传输。
